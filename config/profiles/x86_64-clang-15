version=15

[settings]
arch=x86_64
os=Linux
compiler=clang
compiler.version=$version
compiler.cppstd=20
compiler.libcxx=c++_shared
build_type=Release

[buildenv]
CC=clang-$version
CXX=clang++-$version
LD=lld-$version
AR=llvm-ar-$version
RANLIB=llvm-ranlib-$version
STRIP=llvm-strip-$version

[tool_requires]

[options]

[env]

